{
  "name": "the-kareem-crown",
  "version": "0.1.2",
  "private": true,
  "scripts": {
    "vercel-build": "mkdir -p tmp || true && npm run build --prefix \"Kareem Crown Pre-Portfolio Site Codebase\""
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
